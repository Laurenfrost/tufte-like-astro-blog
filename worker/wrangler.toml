name = "tufte-style-blog-test"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# R2 bucket binding
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "tufte-style-blog-test"

# Cloudflare Image Transformations binding
[images]
binding = "IMAGES"

# Custom domain (configure in Cloudflare dashboard)
# routes = [
#   { pattern = "img.yourdomain.com", custom_domain = true }
# ]
